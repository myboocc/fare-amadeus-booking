apply plugin: 'java'

group = 'com.intlgds'
version = "${versions.amadeus_booking}"
jar {
	baseName = 'amadeus-booking-proto'
}

dependencies {
    compile libraries.thrift
    compile("org.slf4j:slf4j-api:1.7.7") { force = true }
}
